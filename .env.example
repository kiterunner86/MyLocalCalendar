# Supabase (Project Settings -> API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Only used server-side (ingestion scripts, never exposed to the browser)
SUPABASE_SERVICE_ROLE_KEY=

# Anthropic (for later AI-extraction steps — not needed for the skeleton)
ANTHROPIC_API_KEY=
